spring:
  application:
    name: gateway
  cloud:
    gateway:
      server:
        webflux:
          httpclient:
            connect-timeout: 5000
            response-timeout: 30s
          routes:
            - id: auth-route
              uri: http://localhost:8081
              predicates:
                - Path=/api/auth/**

jwt:
  secret: dGhpc0lzQVNlY3JldEtleUZvckpXVFNpZ25pbmcxMjM0NTY3ODkw

server:
  port: 8080

logging:
  level:
    com.ongard: DEBUG
    org.springframework.cloud.gateway: DEBUG
