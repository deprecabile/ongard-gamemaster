1 - creare un nuovo controller in Authentication per avere in output i dati di un utente (username, email) - ROTTA
AUTENTICATA
2 - arricchire pagina account per permettere all'utente di vedere il proprio profilo, cambiare username, cambiare email

Per ora NON mettiamo il cambio password - gli utenti useranno password dimenticata per reimpostarla.

